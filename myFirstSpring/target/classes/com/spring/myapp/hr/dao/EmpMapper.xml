<?xml version="1.0" encoding="UTF-8"?> 
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="com.spring.myapp.hr.dao.IEmpDAO">

<select id="getEmpCount" parameterType="int" resultType="int">
	SELECT COUNT(*) FROM EMPLOYEES
	<if test="deptid !=null">
		WHERE department_id = #{deptid}
	</if>
</select>

<select id ="getEmplist" resultMap="empMap">
	SELECT * FROM EMPLOYEES
</select>

<select id ="getEmpList" parameterType="int" resultMap="empMap">
	SELECT * FROM EMPLOYEES
	<if test = "empid != null">
		WHERE employee_id = #{empid}
	</if>
</select>


<resultMap type="com.spring.myapp.hr.vo.EmployeeVO" id="empMap">
	<result column = "EMPLOYEE_ID" property = "employeeId"/>
	<result column = "FIRST_NAME" property = "firstName"/>
	<result column = "LAST_NAME" property = "lastName"/>
	<result column = "EMAIL" property = "email"/>
	<result column = "PHONE_NUMBER" property = "phoneNumber"/>
	<result column = "HIRE_DATE" property = "hireDate"/>
	<result column = "JOB_ID" property = "jobId"/>
	<result column = "SALARY" property = "salary"/>
	<result column = "COMMISSION_PCT" property = "commissionPct"/>
	<result column = "MANAGER_ID" property = "managerId"/>
	<result column = "DEPARTMENT_ID" property = "departmentId"/>
</resultMap>

<insert id="insertEmp" parameterType="com.spring.myapp.hr.vo.EmployeeVO">
	INSERT into employees 
	VALUES(
	#{employeeId}, #{firstName}, #{lastName}, #{email}, 
	#{phoneNumber}, #{hireDate}, #{jobId}, #{salary}, 
	#{commissionPct}, #{managerId}, #{departmentId})
</insert>

<select id = "getAllJobId" resultType="hashmap">
	SELECT JOB_ID as "jobId",
		   JOB_TITLE as "jobTitle" 
    FROM jobs
</select>

<select id = "getAllManagerId" resultType="hashmap">
	select   d.manager_id as "managerId", e.first_name as "firstName"
	FROM   	 departments d join employees e
	  on 	 d.manager_id = e.employee_id 
	order by d.manager_id
</select>

<select id = "getAllDepId" resultType="hashmap">
	SELECT DEPARTMENT_ID as "departmentId",
		   DEPARTMENT_NAME as "departmentName" 
	FROM departments 
</select>

<delete id="deleteEmp">
	delete FROM employees WHERE employee_id=#{empid} and email=#{email}
</delete>

<delete id="deleteJobHistory" parameterType="int">
	delete FROM job_history WHERE employee_id=#{empid}
</delete>

<update id="updateEmp" parameterType="com.spring.myapp.hr.vo.EmployeeVO">
	UPDATE employees 
		SET FIRST_NAME = #{firstName}, LAST_NAME = #{lastName}, EMAIL= #{email}, 
		PHONE_NUMBER=#{phoneNumber}, HIRE_DATE=#{hireDate}, JOB_ID=#{jobId}, 
		SALARY=#{salary}, COMMISSION_PCT=#{commissionPct}, MANAGER_ID=#{managerId}, 
		DEPARTMENT_ID=#{departmentId} WHERE EMPLOYEE_ID=#{employeeId}
</update>

</mapper> 